fastapi==0.115.0
uvicorn==0.30.6
pdfplumber==0.11.4
google-generativeai==0.8.3
groq>=0.13.0
python-multipart==0.0.9
python-dotenv==1.0.1

# Optional — only needed if you want to upload SCANNED documents (photos or
# scanned PDFs, not typed digital PDFs). Also requires installing the
# Tesseract OCR and Poppler system programs separately — see README.
# Skip these three if you're short on time; the app works fine without them.
pytesseract
pdf2image
pillow>=11.0
